

stop();
stop()
	{
		var element = $('.timer1')
			element.trigger('pause');
			var element = $('.timer2')
			element.trigger('pause');
			var element = $('.timer3')
			element.trigger('pause');
		}
	$('#question-form').html("<%= j render(:partial => 'calculate_marks', locals: {marks: $marks}) %>");